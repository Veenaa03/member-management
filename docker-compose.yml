version: '3.8'
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: surestdb
      POSTGRES_USER: surest
      POSTGRES_PASSWORD: surestpass
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
